<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="\cssfile\styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form  p-10 text-white">
            <form action="/create" method="POST">
                <input class ="block px-5 py-2 bg-zinc-600 w-full rounded-md outline-none" type="text" placeholder="File Name here..." name="title">
                <textarea class ="block px-5 py-5  bg-zinc-600 w-full rounded-md outline-none resize-none mt-2" name="details" placeholder="Write your task details here..."></textarea>
                <input class="mt-3 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create file">
            </form>
        </div>
        <h1 class="text-4xl tracking-tighter text-white pl-11" >Your Files</h1>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){%>
                <% files.forEach(function(val){%>
                    <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a class="text-blue-500 inline-block " href="/files/<%= val %>">Read more</a>
                            <a class="text-zinc-500" href="/edit/<%= val%>">Edit Name</a>
                        </div>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600">No tasks yet</h3>
            <% } %>
        </div>
        
    </div>

    <script src="\jsfiles\script.js"></script>
</body>

</html>